# Open Weather Forecast CLI

![Output Preview Python forecast](preview.png)

1. Install the dependencies
```bash
pipenv install
```

2. Run the application with an argument (city)
```bash
python app.py "São Paulo"
```